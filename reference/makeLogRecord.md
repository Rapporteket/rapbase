# Make a log record

Internal function adding default values and make a formatted log record.

## Usage

``` r
makeLogRecord(content)
```

## Arguments

- content:

  A named list of values to be logged

## Value

A formatted log entry
