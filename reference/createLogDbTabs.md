# Create tables for log entries in a database

Internal function that crates a database tables to be used for logging.
Will return an error if the table(s) already exists.

## Usage

``` r
createLogDbTabs()
```

## Value

Invisibly TRUE
